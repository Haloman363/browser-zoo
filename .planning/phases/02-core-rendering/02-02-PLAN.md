# PLAN: Phase 2.2 - Zoo Editor & UI

---
id: phase-2-2
requirements: [EDITOR-01, EDITOR-02, UI-01]
must_haves:
  truths:
    - "A UI overlay exists with a list of available animals."
    - "Clicking an animal in the UI 'selects' it for placement."
    - "Clicking the grid with a selected animal places a new instance on that tile."
    - "The player can switch between 'Move' and 'Place' modes."
  artifacts:
    - "web/src/core/EditorManager.ts"
    - "web/src/ui/UIManager.ts"
---

## Tasks

<task id="2.2.1" phase="2.2">
  <title>Implementation: UIManager</title>
  <files>
    <file>web/src/ui/UIManager.ts</file>
  </files>
  <action>
    Create a UIManager that handles the HTML/CSS overlay for the zoo controls. It should display a list of animals and allow selection.
  </action>
  <verify>
    <manual>Verify that the animal selection UI appears and buttons are clickable.</manual>
  </verify>
  <done>Functional UI overlay for animal selection.</done>
</task>

<task id="2.2.2" phase="2.2">
  <title>Implementation: EditorManager</title>
  <files>
    <file>web/src/core/EditorManager.ts</file>
  </files>
  <action>
    Implement an EditorManager that tracks the current placement state (what animal is selected) and coordinates with the GridRenderer and AnimalManager to place new instances.
  </action>
  <verify>
    <manual>Select an animal in the UI and click the grid to place multiple instances.</manual>
  </verify>
  <done>Ability to place new animal instances on the grid.</done>
</task>

<task id="2.2.3" phase="2.2">
  <title>Implementation: Scene Integration (v2)</title>
  <files>
    <file>web/src/main.ts</file>
  </files>
  <action>
    Integrate UIManager and EditorManager into the main loop. Handle mode switching (Select/Place).
  </action>
  <verify>
    <manual>Verify end-to-end placement workflow.</manual>
  </verify>
  <done>Complete Zoo Editor prototype functional.</done>
</task>
