# PLAN: Phase 4.1 - Environment & Persistence

---
id: phase-4-1
requirements: [ENV-01, ENV-02, SAVE-01]
must_haves:
  truths:
    - "The player can paint different terrain types (Grass, Sand, Dirt, Water)."
    - "Environmental objects (Trees, Rocks) can be placed like animals but remain static."
    - "The entire zoo state (terrain + animals + objects) persists across browser refreshes."
  artifacts:
    - "web/src/core/TerrainManager.ts"
    - "web/src/core/SceneryManager.ts"
    - "web/src/core/PersistenceManager.ts"
---

## Tasks

<task id="4.1.1" phase="4.1">
  <title>Implementation: Terrain Painting</title>
  <files>
    <file>web/src/core/GridRenderer.ts</file>
    <file>web/src/core/TerrainManager.ts</file>
  </files>
  <action>
    Refactor terrain rendering to support per-tile texture switching. Implement a TerrainManager to track tile types.
  </action>
  <verify>
    <manual>Select 'Sand' in the UI and paint on the grid.</manual>
  </verify>
  <done>Multi-terrain painting system is functional.</done>
</task>

<task id="4.1.2" phase="4.1">
  <title>Implementation: Scenery & Objects</title>
  <files>
    <file>web/src/core/SceneryManager.ts</file>
  </files>
  <action>
    Create a SceneryManager to handle static sprites (trees, rocks). Ensure they use the same Z-sorting as animals.
  </action>
  <verify>
    <manual>Place a tree and walk an animal behind/in front of it to verify Z-sorting.</manual>
  </verify>
  <done>Static environmental objects correctly integrated.</done>
</task>

<task id="4.1.3" phase="4.1">
  <title>Implementation: Persistence (Save/Load)</title>
  <files>
    <file>web/src/core/PersistenceManager.ts</file>
  </files>
  <action>
    Implement a manager that serializes the current zoo state to JSON and stores it in LocalStorage. Load it on startup.
  </action>
  <verify>
    <manual>Place some animals, refresh the page, and verify they are still there.</manual>
  </verify>
  <done>Zoo state persists across sessions.</done>
</task>
