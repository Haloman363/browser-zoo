# PLAN: Phase 2.1 - Core Rendering & Camera

---
id: phase-2-1
requirements: [RENDER-01, RENDER-02, CONTROL-01]
must_haves:
  truths:
    - "A 75x75 grid is rendered with ZT1-accurate grass tiles."
    - "The camera can be panned using the mouse (right-click drag or WASD)."
    - "The camera can be zoomed using the scroll wheel."
    - "An animal instance is correctly positioned on a specific grid tile."
  artifacts:
    - "web/src/core/CameraControls.ts"
    - "web/src/core/GridRenderer.ts"
    - "web/src/main.ts" (updated)
---

## Tasks

<task id="2.1.1" phase="2.1">
  <title>Implementation: Camera Controls</title>
  <files>
    <file>web/src/core/CameraControls.ts</file>
  </files>
  <action>
    Create a CameraControls class that handles panning (WASD or Right-Click drag) and zooming for the OrthographicCamera.
  </action>
  <verify>
    <manual>Open the web app and verify that you can move the view around and zoom in/out.</manual>
  </verify>
  <done>Smooth camera movement and zoom are functional in the Three.js scene.</done>
</task>

<task id="2.1.2" phase="2.1">
  <title>Refactor: GridRenderer</title>
  <files>
    <file>web/src/core/GridRenderer.ts</file>
    <file>web/src/TileManager.ts</file>
  </files>
  <action>
    Refactor TileManager into a more robust GridRenderer that can handle different tile types (terrain) and efficient rendering of a large grid.
  </action>
  <verify>
    <manual>Observe the 75x75 grid with grass tiles and the hover highlight.</manual>
  </verify>
  <done>Grid is efficiently rendered with high-quality tile mapping.</done>
</task>

<task id="2.1.3" phase="2.1">
  <title>Implementation: Scene Integration</title>
  <files>
    <file>web/src/main.ts</file>
  </files>
  <action>
    Update main.ts to use the new CameraControls and GridRenderer. Ensure the animal (buffalo) is still rendered and interactive.
  </action>
  <verify>
    <manual>Run the app and verify all systems (camera, grid, animal) work together.</manual>
  </verify>
  <done>Unified scene with functional controls and rendering.</done>
</task>
